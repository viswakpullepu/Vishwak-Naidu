diff --git a/style.css b/style.css
index 0e26f457..1473edd9 100644
--- a/style.css
+++ b/style.css
@@ -25,12 +25,18 @@
 }
 
 /* --- BASE & UTILITIES --- */
-* {
+*, *::before, *::after {
   margin: 0;
   padding: 0;
   box-sizing: border-box;
 }
 
+html, body {
+  width: 100%;
+  max-width: 100%;
+  overflow-x: hidden;
+}
+
 html {
   scroll-behavior: smooth;
   background-color: var(--bg-color);
@@ -39,7 +45,6 @@ html {
 }
 
 body {
-  overflow-x: hidden;
   position: relative;
   background: radial-gradient(circle at 50% 50%, rgba(224, 96, 49, 0.04) 0%, transparent 80%);
 }
@@ -168,9 +173,10 @@ body.hovering #custom-cursor-ring {
   overflow: hidden;
   font-family: var(--font-display);
   font-weight: 900;
-  font-size: 24px;
-  letter-spacing: 0.1em;
+  font-size: clamp(14px, 5vw, 24px);
+  letter-spacing: 0.08em;
   color: #fff;
+  white-space: nowrap;
 }
 
 .loading-letter {
@@ -296,7 +302,12 @@ nav a:hover, nav a.active {
   background: none;
   border: none;
   color: #fff;
-  font-size: 24px;
+  font-size: 22px;
+  padding: 10px;
+  min-width: 44px;
+  min-height: 44px;
+  align-items: center;
+  justify-content: center;
   cursor: pointer;
 }
 
@@ -307,14 +318,18 @@ nav a:hover, nav a.active {
   right: 0;
   width: 100%;
   height: 100vh;
+  max-height: 100vh;
+  overflow-y: auto;
   background-color: rgba(5, 5, 5, 0.98);
   backdrop-filter: blur(20px);
-  z-index: 999;
+  -webkit-backdrop-filter: blur(20px);
+  z-index: 1005;
   display: flex;
   flex-direction: column;
-  justify-content: center;
+  justify-content: flex-start;
   align-items: center;
-  gap: 40px;
+  padding: 80px 20px 40px 20px;
+  gap: 24px;
   transform: translateX(100%);
   transition: transform 0.6s cubic-bezier(0.85, 0, 0.15, 1);
 }
@@ -340,11 +355,17 @@ nav a:hover, nav a.active {
 .close-mobile-btn {
   position: absolute;
   top: 24px;
-  right: 40px;
+  right: 24px;
   background: none;
   border: none;
   color: #fff;
-  font-size: 32px;
+  font-size: 28px;
+  padding: 8px;
+  min-width: 44px;
+  min-height: 44px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
   cursor: pointer;
 }
 
@@ -401,6 +422,22 @@ nav a:hover, nav a.active {
   z-index: 2;
   pointer-events: none;
 }
+.hero-subtitle-top,
+.hero-title,
+.hero-subtitle,
+.hero-statement,
+.hero-btn {
+  pointer-events: auto;
+}
+
+.hero-statement {
+  font-family: var(--font-body);
+  font-size: 16px;
+  line-height: 1.7;
+  color: var(--text-secondary);
+  max-width: 650px;
+  margin: 0 auto 36px auto;
+}
 
 .hero-subtitle-top {
   font-family: var(--font-accent);
@@ -603,20 +640,131 @@ nav a:hover, nav a.active {
   padding-bottom: 8px;
 }
 
+.quick-facts-grid {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 10px;
+  margin-bottom: 24px;
+}
+
+.fact-item {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  background: rgba(255, 255, 255, 0.03);
+  border: 1px solid var(--border-color);
+  padding: 8px 16px;
+  border-radius: 20px;
+  font-size: 13px;
+  font-weight: 500;
+  color: var(--text-secondary);
+  transition: var(--transition-fast);
+}
+
+.fact-item i {
+  color: var(--accent-color);
+  font-size: 14px;
+}
+
+.fact-item:hover {
+  background: var(--accent-soft);
+  border-color: var(--accent-color);
+  color: #fff;
+}
+
+/* --- EXPERIENCE & EDUCATION TIMELINE SECTION --- */
+#experience {
+  position: relative;
+}
+
+.timeline-container {
+  position: relative;
+  max-width: 1000px;
+  margin: 0 auto;
+  padding: 40px 0;
+}
+
+.timeline-container::before {
+  content: '';
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  left: 50%;
+  width: 2px;
+  background: linear-gradient(180deg, var(--accent-color) 0%, rgba(224, 96, 49, 0.1) 100%);
+  transform: translateX(-50%);
+}
+
 .timeline-item {
-  margin-bottom: 16px;
+  position: relative;
+  width: 50%;
+  padding: 0 40px 40px 40px;
+  box-sizing: border-box;
+}
+
+.timeline-item:nth-child(odd) {
+  left: 0;
+  text-align: right;
 }
+
+.timeline-item:nth-child(even) {
+  left: 50%;
+  text-align: left;
+}
+
+.timeline-dot {
+  position: absolute;
+  top: 4px;
+  width: 16px;
+  height: 16px;
+  border-radius: 50%;
+  background: var(--bg-color);
+  border: 2px solid var(--accent-color);
+  box-shadow: 0 0 10px var(--accent-glow);
+  z-index: 2;
+}
+
+.timeline-item:nth-child(odd) .timeline-dot {
+  right: -8px;
+}
+
+.timeline-item:nth-child(even) .timeline-dot {
+  left: -8px;
+}
+
+.timeline-content {
+  background: var(--bg-card);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  border: 1px solid var(--border-color);
+  border-radius: 16px;
+  padding: 24px;
+  transition: var(--transition-smooth);
+}
+
+.timeline-content:hover {
+  border-color: var(--border-color-hover);
+  box-shadow: 0 10px 30px -10px rgba(224, 96, 49, 0.15);
+}
+
 .timeline-title {
-  font-weight: 600;
-  font-size: 14px;
+  font-family: var(--font-display);
+  font-size: 18px;
+  font-weight: 700;
+  color: #fff;
+  margin-bottom: 6px;
 }
+
 .timeline-meta {
-  font-size: 12px;
-  color: var(--text-muted);
-  margin-bottom: 4px;
+  font-size: 13px;
+  font-weight: 500;
+  color: var(--accent-color);
+  margin-bottom: 12px;
 }
+
 .timeline-desc {
-  font-size: 12px;
+  font-size: 14px;
+  line-height: 1.6;
   color: var(--text-secondary);
 }
 
@@ -669,6 +817,115 @@ nav a:hover, nav a.active {
   transform: translateY(-2px);
 }
 
+/* --- SKILLS / MY ARSENAL SECTION --- */
+.skills-container {
+  display: flex;
+  flex-direction: column;
+  gap: 40px;
+  width: 100%;
+}
+
+@media (min-width: 1024px) {
+  .skills-container {
+    flex-direction: row;
+    align-items: flex-start;
+  }
+  .sphere-wrapper {
+    flex: 1;
+    position: sticky;
+    top: 100px;
+    display: flex;
+    justify-content: center;
+  }
+  .skills-cards-column {
+    flex: 1.2;
+  }
+}
+
+.skills-cards-column {
+  display: flex;
+  flex-direction: column;
+  gap: 24px;
+  width: 100%;
+}
+
+.skill-category-card {
+  padding: 30px;
+}
+
+.skill-category-title {
+  margin-bottom: 24px;
+  font-size: 18px;
+  color: var(--text-primary);
+  border-bottom: 1px solid rgba(255,255,255,0.05);
+  padding-bottom: 12px;
+  text-transform: uppercase;
+  letter-spacing: 1px;
+}
+
+.skill-grid-items {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 12px;
+}
+
+.skill-item-box {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  background: rgba(255, 255, 255, 0.03);
+  border: 1px solid rgba(255, 255, 255, 0.05);
+  padding: 8px 16px;
+  border-radius: 8px;
+  font-size: 14px;
+  font-weight: 500;
+  color: var(--text-secondary);
+  transition: all 0.3s ease;
+  cursor: pointer;
+}
+
+.sphere-wrapper {
+  width: 100%;
+  max-width: 100%;
+  overflow: hidden;
+  display: flex;
+  justify-content: center;
+  align-items: center;
+}
+#skill-sphere {
+  max-width: 100%;
+  display: flex;
+  justify-content: center;
+  align-items: center;
+}
+#skill-sphere canvas, .tagcloud {
+  max-width: 100% !important;
+}
+
+.skill-item-box i {
+  font-size: 22px;
+  width: 22px;
+  height: 22px;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+}
+
+.skill-item-box img {
+  width: 22px !important;
+  height: 22px !important;
+  object-fit: contain;
+  border-radius: 4px;
+}
+
+.skill-item-box:hover {
+  background: rgba(255, 255, 255, 0.08);
+  border-color: var(--accent-color);
+  color: #fff;
+  transform: translateY(-3px);
+  box-shadow: 0 4px 12px rgba(224, 96, 49, 0.15);
+}
+
 @media (max-width: 900px) {
   .about-grid {
     grid-template-columns: 1fr;
@@ -681,7 +938,7 @@ nav a:hover, nav a.active {
 /* --- PROJECTS SECTION --- */
 .projects-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
+  grid-template-columns: repeat(auto-fill, minmax(min(280px, 100%), 1fr));
   gap: 32px;
 }
 
@@ -690,11 +947,14 @@ nav a:hover, nav a.active {
   padding: 0;
   display: flex;
   flex-direction: column;
+  height: 100%;
 }
 
 .project-image-box {
   width: 100%;
-  height: 200px;
+  aspect-ratio: 16 / 9;
+  max-height: 200px;
+  height: auto;
   overflow: hidden;
   position: relative;
 }
@@ -728,9 +988,10 @@ nav a:hover, nav a.active {
 
 .project-info {
   padding: 24px;
-  flex-grow: 1;
+  flex: 1 1 auto;
   display: flex;
   flex-direction: column;
+  justify-content: space-between;
 }
 
 .project-title {
@@ -744,19 +1005,33 @@ nav a:hover, nav a.active {
   line-height: 1.6;
   color: var(--text-secondary);
   margin-bottom: 20px;
-  flex-grow: 1;
+  flex: 1 1 auto;
 }
 
 .project-links {
   display: flex;
-  gap: 16px;
+  align-items: center;
+  gap: 12px;
+  margin-top: auto;
 }
 .project-link {
-  font-size: 18px;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  min-width: 44px;
+  min-height: 44px;
+  padding: 10px;
+  font-size: 20px;
+  border-radius: 50%;
+  background: rgba(255, 255, 255, 0.04);
+  border: 1px solid var(--border-color);
   color: var(--text-secondary);
+  transition: var(--transition-fast);
 }
 .project-link:hover {
-  color: var(--accent-color);
+  background: var(--accent-soft);
+  border-color: var(--accent-color);
+  color: #fff;
 }
 
 /* --- AWARDS & ACHIEVEMENTS SECTION --- */
@@ -961,6 +1236,20 @@ nav a:hover, nav a.active {
   outline: none;
   box-shadow: 0 0 15px rgba(224, 96, 49, 0.1);
 }
+.form-group input:focus-visible, .form-group textarea:focus-visible {
+  outline: 2px solid var(--accent-color);
+  outline-offset: 2px;
+}
+.form-group.error input, .form-group.error textarea {
+  border-color: #ef4444 !important;
+  background: rgba(239, 68, 68, 0.05);
+}
+.field-error-msg {
+  color: #ef4444;
+  font-size: 12px;
+  margin-top: 4px;
+  display: block;
+}
 
 .submit-btn {
   background: var(--accent-soft);
@@ -1103,46 +1392,49 @@ footer {
   text-align: center;
 }
 .footer-text {
-  font-size: 12px;
-  color: var(--text-muted);
-}
-
-/* --- ANIMATION ON SCROLL REVEALS --- */
-.reveal-on-scroll {
-  /* GSAP handles opacity for children, so parent shouldn't hide them */
+  font-size: 13px;
+  color: var(--text-secondary);
 }
-.reveal-on-scroll.visible {
-  opacity: 1;
-  transform: translateY(0);
+.footer-socials {
+  display: none;
+  justify-content: center;
+  align-items: center;
+  gap: 16px;
+  margin-bottom: 20px;
 }
-
-/* --- TECH STACK SPHERE --- */
-.tools-container {
+.footer-socials a {
+  background: var(--bg-card);
+  border: 1px solid var(--border-color);
+  color: var(--text-secondary);
+  width: 44px;
+  height: 44px;
+  min-width: 44px;
+  min-height: 44px;
+  border-radius: 50%;
   display: flex;
-  justify-content: center;
   align-items: center;
-  margin: 40px auto;
-  width: 100%;
-  overflow: hidden; /* Prevent horizontal scroll on small devices */
+  justify-content: center;
+  font-size: 18px;
+  transition: var(--transition-fast);
 }
-.tools-sphere {
-  font-family: var(--font-display);
-  font-weight: 600;
-  color: var(--accent-color);
-  text-shadow: 0 0 10px rgba(0, 243, 255, 0.4);
+.footer-socials a:hover {
+  background: var(--accent-color);
+  border-color: var(--accent-color);
+  color: #fff;
 }
-.tagcloud {
+footer img {
+  max-width: 100%;
+  height: auto;
   display: inline-block;
-  margin: 0 auto;
 }
-.tagcloud--item {
-  padding: 2px 4px;
-  background-color: transparent;
-  cursor: pointer;
+
+/* --- ANIMATION ON SCROLL REVEALS --- */
+.reveal-on-scroll {
+  /* GSAP handles opacity for children, so parent shouldn't hide them */
 }
-.tagcloud--item:hover {
-  color: #fff;
-  text-shadow: 0 0 15px var(--accent-color);
+.reveal-on-scroll.visible {
+  opacity: 1;
+  transform: translateY(0);
 }
 
 /* --- RESPONSIVE SCREEN OPTIMIZATIONS (ALL BREAKPOINTS) --- */
@@ -1163,17 +1455,13 @@ footer {
   .info-cards-grid {
     gap: 16px;
   }
-}
-
-/* 3. Small Laptops & Tablets Landscape (under 1024px) */
-@media (max-width: 1024px) {
   .floating-socials {
-    display: none; /* Hide sidebar socials on tablet views */
+    display: none;
   }
 }
 
-/* 4. Tablets Portrait & Smaller Screens (under 768px) */
-@media (max-width: 768px) {
+/* 3. Small Laptops & Tablets Landscape (under 1024px) */
+@media (max-width: 1024px) {
   header {
     padding: 16px 24px;
   }
@@ -1181,8 +1469,18 @@ footer {
     display: none !important;
   }
   .mobile-nav-toggle {
-    display: block !important;
+    display: flex !important;
+  }
+  .floating-socials {
+    display: none;
   }
+  .footer-socials {
+    display: flex;
+  }
+}
+
+/* 4. Tablets Portrait & Smaller Screens (under 768px) */
+@media (max-width: 768px) {
   section {
     padding: 60px 16px;
   }
@@ -1192,6 +1490,15 @@ footer {
   .section-title {
     font-size: 32px;
   }
+  .form-group input,
+  .form-group textarea {
+    font-size: 16px;
+  }
+  #hero {
+    min-height: auto;
+    padding-top: 100px;
+    padding-bottom: 60px;
+  }
   
   /* Stacking columns */
   .about-grid {
@@ -1207,9 +1514,22 @@ footer {
   
   /* Centering stacked card elements to keep visual balance */
   .project-card, .award-card, .community-card {
-    max-width: 500px;
-    margin-left: auto;
-    margin-right: auto;
+    max-width: 100%;
+  }
+
+  .timeline-container::before {
+    left: 20px;
+  }
+  .timeline-item {
+    width: 100% !important;
+    left: 0 !important;
+    padding-left: 50px !important;
+    padding-right: 0 !important;
+    text-align: left !important;
+  }
+  .timeline-dot {
+    left: 12px !important;
+    right: auto !important;
   }
 }
 
@@ -1227,6 +1547,12 @@ footer {
 
 /* 6. Mobile Portrait / Standard Phones (under 480px) */
 @media (max-width: 480px) {
+  section {
+    padding: 50px 14px;
+  }
+  .glass-card {
+    padding: 20px 16px;
+  }
   .hero-title {
     font-size: 42px;
     line-height: 1.1;
@@ -1236,6 +1562,12 @@ footer {
     letter-spacing: 0.08em;
     padding: 0 8px;
   }
+  .hero-statement {
+    font-size: 14px;
+    line-height: 1.6;
+    margin-bottom: 28px;
+    padding: 0 10px;
+  }
   .projects-grid {
     grid-template-columns: 1fr;
     gap: 20px;
@@ -1244,26 +1576,68 @@ footer {
     grid-template-columns: 1fr;
     gap: 16px;
   }
-  .about-photo {
-    max-width: 240px;
+  .about-image-container {
+    max-width: 260px;
     margin: 0 auto 24px auto;
   }
+  .skill-category-card {
+    padding: 18px 14px;
+  }
+  .skill-grid-items {
+    gap: 8px;
+  }
+  .skill-item-box {
+    padding: 6px 10px;
+    font-size: 12px;
+    gap: 6px;
+  }
   .logo {
     font-size: 20px;
   }
+  .timeline-container::before {
+    left: 12px;
+  }
+  .timeline-item {
+    padding-left: 36px !important;
+    padding-bottom: 24px !important;
+  }
+  .timeline-dot {
+    left: 4px !important;
+    width: 14px;
+    height: 14px;
+  }
+  .timeline-content {
+    padding: 16px;
+  }
+  .timeline-title {
+    font-size: 16px;
+  }
+  .timeline-meta {
+    font-size: 12px;
+  }
+  .timeline-desc {
+    font-size: 13px;
+  }
 }
 
 /* 7. Extra-Small Mobile Devices (under 360px) */
 @media (max-width: 360px) {
+  header {
+    padding: 12px 16px;
+  }
   .hero-title {
     font-size: 34px;
   }
   .logo {
-    font-size: 18px;
+    font-size: 16px;
   }
   .loading-text {
     font-size: 16px;
   }
+  .sphere-wrapper {
+    transform: scale(0.85);
+    margin: -20px 0;
+  }
 }
 
 
@@ -1292,8 +1666,8 @@ html.lenis, html.lenis body {
   pointer-events: none;
 }
 
-/* --- DECK OF CARDS FANNING LAYOUT & MODAL SYSTEM --- */
-.awards-grid {
+/* --- DECK OF CARDS FANNING LAYOUT, VERCEL GRID & MODAL SYSTEM --- */
+#certifications .awards-grid {
   display: flex !important;
   flex-direction: row;
   justify-content: center;
@@ -1305,6 +1679,109 @@ html.lenis, html.lenis body {
   overflow: visible !important;
 }
 
+#highlights .awards-grid {
+  display: grid !important;
+  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
+  gap: 32px;
+  min-height: auto;
+  padding: 0;
+  margin-top: 40px;
+  position: relative;
+  overflow: visible !important;
+}
+
+/* Vercel Grid & Card Styles */
+.vercel-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
+  gap: 24px;
+  margin-top: 30px;
+}
+
+.vercel-card {
+  background: var(--bg-card);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  border: 1px solid var(--border-color);
+  border-radius: 16px;
+  padding: 24px;
+  display: flex;
+  flex-direction: column;
+  justify-content: space-between;
+  transition: var(--transition-smooth);
+}
+
+.vercel-card:hover {
+  border-color: var(--border-color-hover);
+  box-shadow: 0 10px 30px -10px rgba(224, 96, 49, 0.15);
+  transform: translateY(-4px);
+}
+
+.vercel-card-header {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  margin-bottom: 12px;
+}
+
+.vercel-card-title {
+  font-family: var(--font-display);
+  font-size: 18px;
+  font-weight: 700;
+  color: #fff;
+  display: flex;
+  align-items: center;
+  gap: 10px;
+}
+
+.vercel-status {
+  display: flex;
+  align-items: center;
+  gap: 6px;
+  font-size: 12px;
+  color: #10b981;
+  font-weight: 500;
+}
+
+.vercel-status-dot {
+  width: 8px;
+  height: 8px;
+  background: #10b981;
+  border-radius: 50%;
+  box-shadow: 0 0 8px #10b981;
+}
+
+.vercel-card-desc {
+  font-size: 13px;
+  color: var(--text-secondary);
+  line-height: 1.5;
+  margin-bottom: 20px;
+}
+
+.vercel-card-footer {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  border-top: 1px solid rgba(255, 255, 255, 0.05);
+  padding-top: 14px;
+}
+
+.vercel-link-btn {
+  background: var(--accent-soft);
+  border: 1px solid var(--accent-color);
+  color: #fff;
+  padding: 6px 14px;
+  border-radius: 20px;
+  font-size: 12px;
+  font-weight: 600;
+  cursor: pointer;
+  transition: var(--transition-fast);
+}
+
+.vercel-link-btn:hover {
+  background: var(--accent-color);
+}
+
 .award-card {
   width: 250px;
   height: 330px;
@@ -1443,7 +1920,7 @@ html.lenis, html.lenis body {
 
 /* Responsiveness: Horizontal overlapping scroll for Mobile/Tablets */
 @media (max-width: 1024px) {
-  .awards-grid {
+  #certifications .awards-grid {
     flex-wrap: nowrap !important;
     justify-content: flex-start !important;
     overflow-x: auto !important;
@@ -1454,16 +1931,16 @@ html.lenis, html.lenis body {
     gap: 0;
   }
   
-  .awards-grid::-webkit-scrollbar {
+  #certifications .awards-grid::-webkit-scrollbar {
     height: 6px;
   }
   
-  .awards-grid::-webkit-scrollbar-thumb {
+  #certifications .awards-grid::-webkit-scrollbar-thumb {
     background: rgba(255, 255, 255, 0.15);
     border-radius: 10px;
   }
   
-  .award-card {
+  #certifications .award-card {
     flex: 0 0 230px !important;
     height: 310px !important;
     margin-left: -90px !important;
@@ -1471,11 +1948,11 @@ html.lenis, html.lenis body {
     transform: rotate(0deg) translateY(0px) !important; /* Neutralized fan on touch devices for swipe clarity */
   }
   
-  .award-card:first-child {
+  #certifications .award-card:first-child {
     margin-left: 0 !important;
   }
   
-  .award-card:hover {
+  #certifications .award-card:hover {
     transform: translateY(-20px) scale(1.08) !important;
   }
   
